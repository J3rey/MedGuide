// Later: replace this stub with ML Kit or Gemini vision OCR.
// eslint-disable-next-line @typescript-eslint/no-unused-vars, no-unused-vars
export async function extractTextFromImage(_uri: string): Promise<string> {
  // MVP stub: pretend OCR found this text.
  // Replace with real OCR later.
  return `
    PANADOL OSTEO 665mg
    Paracetamol
  `.trim();
}
